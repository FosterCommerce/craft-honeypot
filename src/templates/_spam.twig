{% extends "_layouts/message" %}
{% set title = "Spam detected"|t('app') %}

{% block message %}
	<h2>{{ title }}</h2>
	<p>{{ (message ?? null) ?: "The requested URL was not found on this server."|t('app') }}</p>
{% endblock %}
